#!/bin/bash

convert() {
  for i in `ls $1`
  do
    if [ -d $1/$i ]
    then
      convert $1/$i
    else
      iconv -f utf-8 -t latin1 -o $1/${i}l $1/$i
      rename -f 's/l//' $1/*
    fi
  done
}

convert $1
